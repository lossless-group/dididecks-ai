<script lang="ts">import SeoContentPage from '$lib/components/marketing/SeoContentPage.svelte';</script>
<SeoContentPage eyebrow="Feature" title="Smart edit for controlled deck revisions" copy="Change persistent fields and review impact before the deck state is rebuilt or exported." ctas={[{ label: 'Advisor workflow', href: '/advisors' }, { label: 'Deck versions', href: '/features/deck-versions', tone: 'ghost' }]} sections={[{ title: 'Why it matters', items: [{ title: 'Field-aware changes', body: 'A single edit can affect several slides, so the impact needs to be visible first.' }, { title: 'Safer acceptance', body: 'Smart edit creates an explicit review point instead of silent mutation.' }, { title: 'Better rebuild flow', body: 'It fits into versions, audit, and export logic rather than bypassing them.' }] }]} />

