# Managed AI Provider MVP

The admin AI provider screen is a repo-local MVP surface only.

- Do not commit real keys.
- Do not return full secrets to the frontend after save.
- Production storage and encryption are still required.
- This page currently represents provider status and admin workflow shape only.
