# Migration Notes

- `apps/frontend` is the new active SvelteKit product app.
- `apps/deck-shell` remains an Astro reference and integration package.
- `splash` remains the Astro landing and messaging reference.
- `context-v` remains the product and architecture source material.
- `client-sites` remain intact and untouched.
