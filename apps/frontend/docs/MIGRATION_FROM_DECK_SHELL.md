# Migration From Deck Shell

- `apps/deck-shell` remains the Astro integration/reference package.
- `apps/frontend` is the active SvelteKit product app.
- The SvelteKit app does not overwrite or repurpose the Astro package.
- Product concepts can be borrowed, but code is not copied wholesale.
